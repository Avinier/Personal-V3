import React, { useState } from 'react';
import { motion } from 'framer-motion';

export default function AchievementsBoard () {
    const [hover, setHover] = useState(false);

    return (
        <section className='w-[250px] h-[300px] bg-gray'>
        </section>
    )
}